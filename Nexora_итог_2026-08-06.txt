Готово — все четыре задачи выполнены и проверены. Итог:

== Что сделано ==

1. policy.html приведена к реальности. Текст переписан под фактические функции
   сайта: какие данные собираются (аккаунт, соцвходы VK/Telegram/Google, профиль,
   медиа), способы входа (email+код, пароль, соцсети), где хранятся данные
   (localStorage браузера + сервер, пароли хешируются SHA-256), кому передаются
   (провайдеры входа, SMTP, платёжные — с пометкой «в тестовом режиме»), права,
   контакты. Даты обновлены на 6 августа 2026.

2. server/server.js ужесточён. Демо-ключ nexora-admin-demo-key убран: теперь ключ
   берётся из env ADMIN_TOKEN -> файла server/.admin-token -> генерируется (nx_...).
   CORS ограничен тремя доверенными origin (localhost:3001 / 127.0.0.1:3001 /
   прод-домен) — чужие сайты больше не получат доступ. Неверный ключ -> понятный
   403 вместо тихого пропуска. Проверено тестами: всё 5/5.

3. Синхронизация на Vercel — /api/save + /api/load на хранилище Vercel Blob
   (private). Логика повторяет сервер: регистрации/заявки/тикеты сливаются по
   уникальным ключам (не затирают друг друга), админские данные — только с
   валидным ADMIN_TOKEN, неверный ключ -> 403. Проверено на проде: merge и
   дедупликация работают, страницы отвечают 200.

4. Деплой выполнен -> https://nexora-zeta-ten.vercel.app (все страницы 200).

== ВАЖНО: вставьте ключи в админке ==

В админке -> Настройки -> «Ключ доступа» введите соответствующий ключ:

  https://nexora-zeta-ten.vercel.app (прод)  -> nx_62929115addc7664c486af3002a0cc97b7f1ff685d6132d7
  localhost:3001 (локально)                   -> nx_28f193b8eda207671a5e8e1e248a4aba5e289f3a46c14e92

Если ключ не вставить, турниры/новости/юзеры НЕ будут сохраняться на сервере
(локально в браузере останутся, но перестанут синхронизироваться между
устройствами). При неверном ключе — предупреждение с подсказкой.

Не забудьте Ctrl+F5 после входа, чтобы браузер отдал новые версии скриптов.

Прод-ключ также сохранён в .admin-token-prod.txt (исключён из деплоя и git).
